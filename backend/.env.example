# Скопируйте этот файл в backend/.env и заполните значения
# ВАЖНО: Не коммитьте реальные секреты в репозиторий!

# PayPal API credentials (sandbox or live depending on PAYPAL_API in code)
PAYPAL_CLIENT=your-paypal-client-id
PAYPAL_SECRET=your-paypal-secret

# Optional: override DB file path (defaults to 'shift_backend.db')
# DATABASE_FILE=shift_backend.db

# Optional: PayPal API base (sandbox by default in the code)
# PAYPAL_API=https://api-m.sandbox.paypal.com
